# add-mcp-server

Foundation F1 from the 5-year strategy: a tenant-scoped, audited MCP server mounted inside the existing FastAPI app, exposing read tools over devices/telemetry/alarms plus one approval-gated write tool — competing on governance, not tool count
