# add-vendor-payload-simulator

Raw-payload device simulator that encodes synthetic uplinks through each device type's real decoder spec, so a new vendor preset can be proven end-to-end without waiting on real hardware
