# add-visual-automation-canvas

Adopt React Flow (`@xyflow/react`) as the platform's one node-graph renderer, and use it to show the automation graph that already exists in the data — trigger → conditions → logic → alarm → notification channels — as a canvas instead of three disconnected form pages
