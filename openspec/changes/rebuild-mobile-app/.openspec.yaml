schema: spec-driven
created: 2026-08-05
goal: A cross-platform iOS+Android client that reuses the web app's API contract
  with zero hardcoded values, authorable by a non-developer through OpenSpec
  tasks
