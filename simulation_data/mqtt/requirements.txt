paho-mqtt==1.6.1
psycopg2-binary==2.9.9
PyYAML>=6.0.1

# shared/payload_codec (LoRaWAN publisher's encode() call) is installed
# separately by start.sh/start.bat and by the Dockerfile — not listed here
# as an editable path, since that only resolves when run from this
# directory on the host and breaks inside the Docker build context.

# Bridge UI dependencies
flask>=3.0.0
flask-socketio>=5.3.6
eventlet>=0.35.0
requests>=2.31.0
