paho-mqtt==1.6.1
psycopg2-binary==2.9.9
PyYAML>=6.0.1

# Local package, same one api/processor install — for the LoRaWAN publisher's
# encode() call (see simulator.py's LoRaWANPublisher).
-e ../../shared/payload_codec

# Bridge UI dependencies
flask>=3.0.0
flask-socketio>=5.3.6
eventlet>=0.35.0
requests>=2.31.0
